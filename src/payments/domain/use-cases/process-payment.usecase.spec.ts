import { ProcessPaymentUsecase } from './process-payment.usecase';

describe('ProcessPaymentUsecase', () => {
  it('should be defined', () => {
    expect(new ProcessPaymentUsecase()).toBeDefined();
  });
});
