/* eslint-disable @typescript-eslint/no-unsafe-call */
import { Workflows } from './workflows';

describe('Workflows', () => {
  it('should be defined', () => {
    expect(new Workflows()).toBeDefined();
  });
});
